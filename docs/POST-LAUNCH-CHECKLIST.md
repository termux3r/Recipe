# Post-Launch Checklist

- Verify homepage loads under 1 second locally
- Confirm add/delete recipe flows work
- Check `/health` endpoint returns 200
- Review database entries with `npm run db:view`
- Tag a release after stable testing
