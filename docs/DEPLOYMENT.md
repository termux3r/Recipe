# Deployment Notes

## Quick Publish Checklist

1. Ensure `npm install` succeeds
2. Run app locally with `npm start`
3. Verify home page and delete flow
4. Set environment variable `PORT` in hosting provider
5. Keep SQLite data persistence path configured
